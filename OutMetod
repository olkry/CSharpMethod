// Метод с текстом, например, авторство в конце.
//Method with text, for example, authorship at the end.
void Method1()
{
      Console.WriteLine("Автор ...");
}


//Метод выводящий резултат
// method print result to console
void PrintData(int num, string msg)
{
      Console.WriteLine(num + msg);
}

//Вывод сообщения несколько раз
//Displaying a message multiple times
void Method21(string msg, int count)
{
      int i = 0;
      while (i < count)
      {
            Console.WriteLine(msg);
            i++;
      }
}

// Как пример, будет возвращать год.
//As an example, it will return the year.
int Method3()
{
      return DateTime.Now.Year;
}

//Метод для выведения на экран массива
// Method for displaying an array
void PrintArray(int[] array)
{
      int count = array.Length;

      for (int i = 0; i < count; i++)
      {
            Console.Write($"{array[i]} ");
      }
      Console.WriteLine();
}


//Метод выведения таблицы разрядов
//Degree table derivation method
string LineBuilder(int num, int pow)
{
      string res = String.Empty;
      for (int i = 1; i <= num; i++)
      {
            res = res + Math.Pow(i, pow) + "\t";
      }
      return res;
}